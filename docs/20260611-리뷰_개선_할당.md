# 1~4차 리뷰 개선 태스크 할당

> 기준 문서: `202606080939`, `202606090913`, `202606100914`, `202606110926` 리뷰
> ✅ 완료 · 🔴 필수 · 🟡 제안 · 🔸 부분 해결

---

## 대도링 (김도혁 @DoHyuk-Centric)

| 차수 | 항목 | 우선순위 | 상태 |
|------|------|----------|------|
| 1차 | `useChatMessages.ts:38` — 읽음처리 `useEffect` 의존성 배열에 `chatId`, `currentUser.id` 추가 | 🔴 필수 | ⬜ 미완 |
| 1차 | 낙관적 메시지 교체를 `tempId` 기준으로 | 🔴 필수 | ✅ 완료 |
| 1차 | `useChatRoomRealtime` 의존성 — `currentUser`, `partner` 객체 → `.id` 원시값으로 | 🟡 제안 | ⬜ 미완 |
| 1차 | `useSpamPrevention` `setInterval` cleanup (`clearInterval`) 추가 | 🟡 제안 | ⬜ 미완 |
| 3차 | Notion 외부 API 실패 시 fallback — `try/catch` 또는 `terms/error.tsx` | 🔴 필수 | ⬜ 미완 |
| 3차 | `process.env.NOTION_*!` non-null 단언 → 필수 env 시작 시 검증 또는 fallback | 🟡 제안 | ⬜ 미완 |
| 4차 | `usedgoods/[id]/page.tsx` — 브라우저 클라 → `createSupabaseServerClient` 전환 | 🔴 필수 | ⬜ 미완 |
| 4차 | `currentUserId` 경로 단일화 — 서버(`getLikeList` 반환)로 통일, `useCurrentUserId` 사용처 정리 | 🟡 제안 | ⬜ 미완 |
| 4차 | `getLikeList` 호출부 (`usedgoods` / `job` / `rental` / `main`) 구조분해 수정 | 🔴 필수 | ✅ 완료 |
| 공통 | 커밋 전 `npx tsc --noEmit` 습관화 (반환형 바꿀 때 특히) | 🔴 필수 | ⬜ 미완 |

---

## 대장님 (박소유 @soyu)

| 차수 | 항목 | 우선순위 | 상태 |
|------|------|----------|------|
| 1차 | `useLikes` 낙관적 업데이트 실패 시 롤백 | 🔴 필수 | ✅ 완료 |
| 1차 | `common_likes` RLS — "본인 `user_id`로만 insert" 정책 추가 | 🔴 필수 | ⬜ 미완 |
| 1차 | `Header` mock 유저 → 실제 로그인 상태 연결 (`authStore` 또는 서버에서 user prop 전달) | 🟡 제안 | ⬜ 미완 |
| 2차 | `CreateJobForm` 작성 완료 후 `/jobs` → `/job` 라우트 수정 | 🔴 필수 | ⬜ 미완 |
| 2차 | `jobs` insert 실패 시 `posts` 고아 행 정리 (이미지 실패와 동일한 보상 패턴) | 🔴 필수 | ⬜ 미완 |
| 1~4차 | `users` RLS — `email`/`phone`/`auth_id` 노출 차단 (4회 연속 미해결) | 🔴 필수·최우선 | ⬜ 미완 |
| 4차 | GitHub Actions `npx tsc --noEmit` 워크플로우 추가 (머지 전 자동 검증) | 🔴 필수 | ⬜ 미완 |
| 4차 | 새 패턴/리팩토링 생길 때 PR 설명 또는 5분 공유 팀 규칙화 | 🟡 제안 | ⬜ 미완 |

---

## 헬근 (이동근)

| 차수 | 항목 | 우선순위 | 상태 |
|------|------|----------|------|
| 1~4차 | `/chats`·`/jobs` 404 — `routes.ts` 상수화 후 `Header.tsx:30,313,405` / `Footer.tsx:6` / `UsedGoodsDetail.tsx:178` / `CreateJobForm.tsx:134` 교체 | 🔴 필수·최우선 | ⬜ 미완 |
| 1~2차 | `UsedGoodsDetail.tsx` "채팅하기" → `/chats` 수정 + 채팅방 생성·이동 로직 (김도혁과 협업) | 🔴 필수 | ⬜ 미완 |
| 1차 | `UsedGoodsDetail.tsx` 내 인라인 `interface UsedGoods` → 공용 `UsedGoodsWithPost` 타입으로 교체 | 🟡 제안 | ⬜ 미완 |
| 1차 | `UsedGoodsDetail.tsx:145` 가격에 `toLocaleString()` 누락 | 🟡 제안 | ⬜ 미완 |
| 1차 | `signup/page.tsx:297,326,356` — 렌더 중 ref 읽기 eslint 경고 수정 | 🟡 제안 | ⬜ 미완 |
| 1차 | `Pagination` — 3개 고정 → 현재 페이지 주변 표시 방식으로 개선 | 🟡 제안 | ⬜ 미완 |
| 4차 | `discord` 라우트 무인증 보호 — `x-cron-secret` 검증 또는 삭제 | 🔴 필수 | ⬜ 미완 |

---

## 임태린 (임태형 @THLIMM)

| 차수 | 항목 | 우선순위 | 상태 |
|------|------|----------|------|
| 1차 | 서버 클라이언트 유틸 중복 — `lib/supabaseServer.ts` vs `utils/supabase/server.ts` 하나로 통일 | 🔴 필수 | ⬜ 미완 |
| 1차 | `auth/callback` OAuth 실패 시 에러 메시지 표시 + `next` 경로 처리 | 🔴 필수 | ⬜ 미완 |
| 1차 | 소셜 로그인 시 `public.users` 행 생성 로직 확인 (DB 트리거 여부 문서화) | 🟡 제안 | ⬜ 미완 |
| 2차 | `RentalList` 찜 롤백 누락 | 🔴 필수 | ✅ 완료 (LikeButton으로 해결) |
| 2차 | `RentalWithPost` 타입 중복 → `rentalList.ts`/`rentalDetail.ts` 분리 | 🔴 필수 | ✅ 완료 |
| 3차 | `useImageUpload` cleanup stale closure — `imagePreviews`를 `useRef`에 담아 언마운트 시 정리 | 🔴 필수 | ⬜ 미완 |
| 3차 | `handleSubmit` 조용히 중단 → 비어있는 필수값 사용자에게 알림 | 🟡 제안 | ⬜ 미완 |
| 4차 | `getLikeList` 반환형 바꿀 때 `grep`으로 소비측 전체 확인 후 커밋 습관 | 🟡 제안 | ⬜ 미완 |
| 4차 | 새 카드 추가 시 `toggleLike` 롤백 패턴 누락 없는지 확인 | 🟡 제안 | ✅ 완료 (LikeButton으로 해결) |

---

## 공통

| 차수 | 항목 | 우선순위 | 담당 | 상태 |
|------|------|----------|------|------|
| 1차 | `auth_id → users.id` 변환 공통 헬퍼 (`getCurrentUserId`) 통일 | 🟡 제안 | 전원 | 🔸 `useCurrentUserId` 훅으로 부분 해결 |
| 1차 | Zustand 스토어 실제 연결 또는 "미연결" 주석 명시 | 🟡 제안 | 대도링·대장님 | ⬜ 미완 |
| 1~4차 | 클라이언트 직접 DB 쓰기 → 서버 액션으로 점진 전환 | 🟡 제안 | 전원 | ⬜ 미완 |
| 4차 | 머지 전 `npm run build` 규칙화 | 🔴 필수 | 전원 | ⬜ 미완 |
| 4차 | Zustand으로 좋아요 동기화 전환 검토 | 🟡 제안 | 대도링·임태린 | ⬜ 미완 |
| 공통 | `not-found.tsx` 한국어 404 페이지 제작 | 🟡 제안 | 미지정 | ⬜ 미완 |
| 공통 | 루트 `/` 개발용 인덱스 → 실제 홈으로 교체 (배포 전) | 🟡 제안 | 미지정 | ⬜ 미완 |

---

## 우선순위 요약

```
🔴 이번 주 내 반드시 (배포 가능 상태 만들기)
  1. users RLS 차단 (email/phone 노출) ——————————— 대장님   ★ 4차 연속
  2. /chats·/jobs 404 라우트 상수화 ————————————— 헬근     ★ 4차 연속
  3. usedgoods/[id] 서버조회 전환 + tsc 통과 ——— 대도링   ★ 4차 연속
  4. jobs insert 실패 시 posts 고아 정리 ————————— 대장님
  5. discord 무인증 보호 ————————————————————————— 헬근
  6. useImageUpload stale closure 수정 ————————— 임태린
  7. Notion 실패 fallback ———————————————————————— 대도링
  8. useChatMessages 읽음처리 deps 추가 ————————— 대도링
  9. auth/callback 에러 처리 ————————————————————— 임태린
 10. GitHub Actions tsc 게이트 ——————————————————— 대장님

🟡 다음 기능 작업 시 함께
 - common_likes RLS insert 정책 ——————————————— 대장님
 - 서버 클라이언트 유틸 중복 통일 ——————————— 임태린
 - Header 실제 로그인 상태 연결 ————————————— 대장님
 - currentUserId 경로 단일화 ————————————————— 대도링
 - useSpamPrevention setInterval cleanup ————— 대도링
```
