# 헤더에 있던 연필모양을 빼고 각 카테고리 목록페이지에서 글쓰기로 통합을 하고 create 연결할 예정 아직 헤더에서 제거하는 부분만 논의되어 유령페이지로 남아있다 (추후 논의후 해결예정)

---

## 수정 및 리팩토링 내역 (2026-06-14)

### 1. 중고거래 카테고리 추가
- `src/type/usedGoods.ts`
  - `PRODUCT_CATEGORIES`에 `전자기기` 항목 추가 (의류 다음)

---

### 2. 필터 컴포넌트 prop 네이밍 통일
- `givenSearch`는 `given` 접두사 유지, 나머지는 `default` 접두사로 통일
- 수정 파일
  - `src/components/common/SearchBar.tsx` — `defaultLocation` prop 추가
  - `src/app/(user)/usedgoods/_components/UsedGoodsFilters.tsx` — `defaultCategory`, `defaultLocation`
  - `src/app/(user)/job/_components/JobFilters.tsx` — `defaultType`, `defaultLocation`
  - `src/app/(user)/rental/_components/RentalFilters.tsx` — `defaultRoomType`, `defaultLocation`
  - `src/app/(user)/usedgoods/page.tsx` — 동일하게 변경
  - `src/app/(user)/job/page.tsx` — 동일하게 변경
  - `src/app/(user)/rental/page.tsx` — 동일하게 변경

---

### 3. EditButton / DeleteButton 일반화
- **`src/components/common/EditButton.tsx`**
  - 기존: `id: number` prop → `/usedgoods/${id}/edit` 하드코딩
  - 변경: `editPath: string` prop으로 경로를 외부에서 주입
- **`src/components/common/DeleteButton.tsx`**
  - 기존: `/usedgoods` 하드코딩, `confirm()` 사용
  - 변경: `redirectPath: string` prop 추가, `ConfirmModal`로 교체하여 삭제 확인 모달 처리

---

### 4. VerifyAuthor 공통 컴포넌트 생성
- **`src/components/common/VerifyAuthor.tsx`** (신규)
  - `authorAuthId`, `editPath`, `postId`, `redirectPath` prop 수신
  - `useAuthStore`에서 현재 유저의 `auth_id`를 읽어 글 작성자와 비교
  - 내 글이면 수정/삭제 버튼 렌더, 아니면 `null` 반환
  - 기존 각 상세 컴포넌트에 흩어진 `isOwner` 체크 로직을 하나로 통합

---

### 5. UsedGoodsDetail 리팩토링
- **`src/app/(user)/usedgoods/[id]/_components/UsedGoodsDetail.tsx`**
  - 기존 `isOwner` 체크 + `EditButton` / `DeleteButton` 직접 렌더 → `VerifyAuthor`로 교체
  - `user: User | null` prop 제거 (VerifyAuthor가 auth store에서 직접 읽음)
  - 채팅하기 버튼의 `isOwner` 체크는 `storeUser?.auth_id`로 대체
- **`src/app/(user)/usedgoods/[id]/page.tsx`**
  - `user={user}` prop 전달 제거

---

### 6. job / rental 상세 페이지에 수정·삭제 버튼 추가
- **타입 수정**
  - `src/type/rental/rentalDetail.ts` — `RentSellerInfo`에 `auth_id` 필드 추가
  - `src/type/job/jobsDetail.ts` — `JobSellerInfo`에 `auth_id` 필드 추가
- **서비스 쿼리 수정**
  - `src/services/rental/rental.ts` — `RENTAL_DETAIL_SELECT` users 필드에 `auth_id` 추가
  - `src/services/job/jobDetail.ts` — `JOB_DETAIL_SELECT` users 필드에 `auth_id` 추가
- **페이지 적용**
  - `src/app/(user)/job/[id]/page.tsx` — `VerifyAuthor` 카드 상단에 추가
  - `src/app/(user)/rental/[id]/page.tsx` — `BackButton` 옆에 `VerifyAuthor` 추가
