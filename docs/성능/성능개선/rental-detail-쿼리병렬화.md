# 부동산 상세 `/rental/[id]` 성능 개선 — 좋아요·신고 상태 쿼리 병렬화

- **담당자**: 김도혁
- **대상 페이지**: `/rental/[id]` (부동산 상세)
- **관련 검사 문서**: [../성능검사/rental-detail.md](../성능검사/rental-detail.md) — 모바일 LCP 2.8s 🟡 / SI 3.4s, 데스크탑 98점(LCP 0.9s 🟢)
- **우선순위**: P1 <!-- 검사 문서에서 LCP·JS를 P1로 지정 -->
- **상태**: 진행 (코드 반영 완료 · 배포 후 PSI 재측정 필요)
- **관련 커밋**: `395ce07` refactor: 부동산 상세 데이터 쿼리 병렬화

## 1. 문제 정의 (무엇이 · 얼마나 느린가)

- 검사 문서([rental-detail.md](../성능검사/rental-detail.md)) 기준 데스크탑은 98점(CWV 전부 🟢)으로 이미 우수. 남은 여지는 **모바일 LCP 2.8s 🟡 · Speed Index 3.4s**.
- 서버 컴포넌트(`RentalDetail`)가 데이터 조회를 **직렬로** 처리하고 있었음: `getUserLikeReportStatus(postId)`를 먼저 `await`한 뒤에야 관련 매물(related items) 조회 블록으로 넘어감. 두 조회는 서로 의존 관계가 없는데도 순차로 실행돼 **서버 렌더 시간이 두 왕복만큼 쌓임** → 초기 HTML 도달·요소 렌더 지연에 그대로 반영됨.

## 2. PSI Insights (개선 기회)

| Insight 항목 | 디바이스 | 예상 절감 (Est savings) | 이번 개선 반영 |
| --- | :---: | --- | :---: |
| LCP 분석 / Speed Index (서버 렌더 지연분) | 📱 모바일 | (절감치 표기 없음) | ✅ (쿼리 병렬화로 서버 응답 단축) |
| 이미지 전송 개선 (Improve image delivery) | 📱/🖥️ | 188 / 160 KiB | ⬜ (목록 이미지 priority 작업에서 공용 컴포넌트로 처리) |
| 사용하지 않는 자바스크립트 줄이기 | 📱/🖥️ | 255 / 257 KiB | ⬜ (Sentry Replay 지연 로딩 — 별도 작업) |
| 렌더링 차단 요청 (Render-blocking) | 📱/🖥️ | 590 / 240 ms | ⬜ (CSS 추정 — 별도 작업) |
| 레거시 JavaScript | 📱/🖥️ | 15 KiB | ⬜ (별도 작업) |

> 반영: ✅ 이번에 처리 / ⬜ 보류
> 이번 작업은 PSI의 특정 "Est savings" 항목이 아니라 **서버 렌더 시간(요소 렌더링 지연·SI에 반영되는 구간)**을 겨냥한다.

## 3. 개선 전 / 후 지표

> 2026-07-06 로컬 Lighthouse(각 2회) 재측정. 상세는 [rental-detail.md §재측정](../성능검사/rental-detail.md#재측정-배포-후--2026-07-06-로컬-lighthouse-cli-자동-측정-각-2회).

| 지표 | 디바이스 | 개선 전 (검사 문서) | 개선 후 | 변화 |
| --- | :---: | :---: | :---: | :---: |
| Performance 점수 | 📱 모바일 | 86 | 63.5 | **−22.5, 악화** |
| LCP | 📱 모바일 | 2.8s 🟡 | **4.83s 🔴** | +2.03s, 등급 하락 |
| TBT | 📱 모바일 | 370ms | 777ms | +407ms, 악화 |
| Speed Index | 📱 모바일 | 3.4s | 2.78s | −0.62s, 개선 |
| Performance 점수 | 🖥️ 데스크탑 | 98 | 97.5 | 거의 동일 |
| TBT | 🖥️ 데스크탑 | 100ms | 32ms | 개선 |

> 모바일 두 측정(4.69s/4.97s)이 서로 가까워 로컬 도구 노이즈만으로 보기 어렵다. 다만 이 개선(쿼리 병렬화, 서버 렌더 단계)은 클라이언트 LCP/TBT와 직접적 인과가 없어 보여, **이 문서의 변경이 원인이라기보다 usedgoods 등에서도 공통으로 나타난 전역 요인의 영향으로 추정**된다. 실제 PSI로 최종 확인 필요.

## 4. 개선 작업 내용 (문제 → 기대 효과 → 그래서 택한 방법)

### 4-1. 좋아요·신고 상태 조회를 관련 매물 조회와 병렬 실행

- **왜 고쳐야 했나**: `getUserLikeReportStatus`와 관련 매물 조회는 서로 결과를 참조하지 않는 독립 쿼리인데도 순차 `await`로 묶여 서버 렌더 시간이 불필요하게 직렬 누적됐음.
- **개선 안 했을 때**: 두 쿼리 지연 시간이 그대로 더해져(=직렬) 초기 HTML 응답이 늦어지고, 모바일에서 요소 렌더링 지연·SI로 드러남.
- **개선했을 때 (기대)**: 두 쿼리가 겹쳐 실행돼 서버 렌더 시간이 둘 중 **느린 쪽 하나** 수준으로 수렴 → TTFB·초기 렌더 도달 단축 → 모바일 LCP/SI 개선 방향.
- **그래서 택한 방법**: `page.tsx`에서 `getUserLikeReportStatus(postId)`를 **await하지 않고 promise만 먼저 시작**(`const likeStatusPromise = getUserLikeReportStatus(postId)`), 관련 매물 조회 블록을 그대로 지나간 뒤 필요한 지점에서 `const { userId, initialLiked, initialReported } = await likeStatusPromise`로 수확. 관련 매물 조회가 도는 동안 좋아요·신고 조회가 백그라운드에서 함께 진행됨.

> 변경 규모는 작지만(핵심 2줄) 서버 컴포넌트의 waterfall을 제거하는 것이 목적. 관련 매물이 없는 분기(`rental.location` 부재)에서도 promise를 미리 띄워두므로 손해가 없다.

## 5. 검증

- **재측정 (2026-07-06, 로컬 Lighthouse 각 2회)**: 🖥️ 데스크탑은 유지·개선(TBT 100→32ms). **📱 모바일은 LCP 2.8s🟡→4.83s🔴, TBT 370→777ms로 악화** — §3 참고
- **개선 확인 (코드 레벨)**: 서버 로그/트레이스에서 두 쿼리가 순차가 아닌 중첩 구간으로 실행되는지, 렌더 결과(좋아요·신고 상태, 관련 매물)가 기존과 동일한지 로컬 프로덕션 빌드로 확인.
- **완료 조건**: 모바일 악화가 이 개선과 무관한 전역 요인 때문인지 확인하고, 실제 PSI로 최종 재측정해야 완료 처리.
